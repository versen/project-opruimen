# Builds the Astro site and publishes it with GitHub Pages.
# Repo settings: Pages -> Source: "GitHub Actions"; custom domain set there
# (not in a CNAME file). For a review copy, set the repository variable
# PREVIEW=true so the build is noindexed and the Join form is disabled, and
# BASE_PATH=/<repo-name> when it is served as a project site under a sub-path.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          PUBLIC_PREVIEW: ${{ vars.PREVIEW }}
          BASE_PATH: ${{ vars.BASE_PATH }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
